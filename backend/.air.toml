root = "."
tmp_dir = "tmp"

[build]
  bin = "./tmp/server"
  cmd = "go build -o ./tmp/server ."
  delay = 1000
  exclude_dir = ["frontend", "tmp"]
  include_ext = ["go", "tpl", "tmpl", "html"]
  exclude_regex = [".*_test.go"]

[log]
  main_only = true

[misc]
  clean_on_exit = true
